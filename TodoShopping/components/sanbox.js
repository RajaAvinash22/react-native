import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  Button,
  StatusBar,
  FlatList,
  TouchableOpacity,
  TextInput
} from 'react-native';

export default function Sandbox() {
    return(
        <Text>Sandbox</Text>
    )
}

const styles = StyleSheet.create({
    
})